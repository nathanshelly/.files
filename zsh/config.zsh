#! /usr/bin/env zsh

# number of lines of history kept by the shell
HISTSIZE=100000
# file where history is saved
HISTFILE="$HOME/.zsh_history"
# number of lines saved to $HISTFILE
SAVEHIST=$HISTSIZE
